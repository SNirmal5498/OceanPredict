name: oceanpredict_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.2
  file_picker: ^8.1.4
  fl_chart: ^0.69.0
  flutter_map: ^7.0.2
  flutter_map_marker_cluster: ^1.3.6
  latlong2: ^0.9.1

  # Added for PDF generation & Web downloading
  pdf: ^3.10.8
  printing: ^5.13.1

  # Added for Excel (.xlsx) generation
  excel: ^4.0.3

  # Added for clean cross-platform & web anchor file downloads
  universal_html: ^2.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true